{
  "name": "storycode-web",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --no-error-on-unmatched-pattern"
  },
  "devDependencies": {
    "eslint": "^9.39.0",
    "prettier": "^3.6.2"
  }
}
